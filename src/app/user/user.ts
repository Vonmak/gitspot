export class User {
    constructor(
       public name: string, public html_url:string, public avatar_url:string, public login:string, public public_repos:number, public followers:number, public following:number,
    ){}
}
